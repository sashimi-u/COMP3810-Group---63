<!DOCTYPE html>
<html>
<head>
    <title>Tasks</title>
    <style>
        body { font-family: Arial; margin: 20px; }
        .task { border: 1px solid #ddd; padding: 15px; margin: 10px 0; }
        .header { display: flex; justify-content: space-between; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Tasks</h1>
        <div>
            Welcome, <%= user.username %>!
            <a href="/tasks/create" style="margin-right:12px;">Create Task</a>
            <form action="/logout" method="POST" style="display: inline;">
                <button type="submit">Logout</button>
            </form>
        </div>
    </div>
    
    <% tasks.forEach(task => { %>
        <div class="task">
            <h3><%= task.title %></h3>
            <p><%= task.description %></p>
            <p>Priority: <%= task.priority %> | Status: <%= task.status %></p>
            <div style="margin-top:8px;">
                <a href="/tasks/<%= task._id %>/edit">Edit</a>
                <form action="/tasks/<%= task._id %>/delete" method="POST" style="display:inline; margin-left:8px;">
                    <button type="submit">Delete</button>
                </form>
            </div>
        </div>
    <% }); %>
    
    <p><a href="/">← Back to Home</a></p>
</body>
</html>